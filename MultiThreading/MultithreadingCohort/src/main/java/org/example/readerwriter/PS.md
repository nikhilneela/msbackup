Implement a readers writers problem
- There are multiple readers and multiple writers
- Only a single writer is allowed to access the critical section
- Multiple readers are allowed to access the cricitcal section
- When a writer is accessing the critical section, no other readers/writers should be allowed
- When a reader is accessing the critical section, more readers can be allowed but writer should not be allowed